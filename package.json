{
  "name": "nuxt-template",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "run-p lint format:check",
    "openapi": "openapi"
  },
  "devDependencies": {
    "@lingxiteam/env": "^1.0.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.1.2"
  }
}
